class Solution {
    public boolean rotateString(String s, String goal) {

        // Rotated string must have the same length
        if (s.length() != goal.length()) {
            return false;
        }

        // Every rotation of s exists inside s + s
        String doubled = s + s;

        return doubled.contains(goal);
    }
}
